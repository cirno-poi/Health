<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"><!--NoActionBar-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
<!--        <item name="android:windowBackground">@color/wbg</item>
        <item name="android:windowActionBar">false</item>

        <item name="android:actionMenuTextColor">@color/mtc</item>
        <item name="android:actionBarSize">@dimen/actionbar_height</item>-->
    </style>

</resources>
